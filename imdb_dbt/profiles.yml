imdb_dbt:

  target: local
  outputs:
    local:
      type: postgres
      threads: 1
      host: localhost
      user: postgres
      password: postgres
      port: 5432
      dbname: postgres
      schema: main
